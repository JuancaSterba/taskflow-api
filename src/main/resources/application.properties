# Activa el perfil de desarrollo. Spring ahora leer� este archivo Y application-dev.properties
spring.profiles.active=dev

# Propiedades generales que no son secretas
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Establece el nivel de log a DEBUG para ver m�s detalles en el framework web y de seguridad
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG

# Esta propiedad es muy �til: fuerza a que el stack trace del error se muestre siempre
server.error.include-stacktrace=always

# Esta propiedad permite ver el log en colores
spring.output.ansi.enabled=always